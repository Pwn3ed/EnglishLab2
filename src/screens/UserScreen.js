import { Text, View, Button } from "react-native";

export function UserScreen({ navigation}) {
    return (
        <View className='flex-1 items-center justify-center bg-purple-950'>
            <Text className='text-white'>User Screen</Text>
        </View>
    )
}
